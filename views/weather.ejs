<%- include('./partials/header'); %>
    <div class="content-container">
    <h1><%= pageTitle %></h1>
    <div class="form-container">
    <form action="/weather" method="post">
        <input type="text" name="city" placeholder="Type in City">
        <button type="submit">Get Weather</button>
    </form>
    </div>
    <% if (apiData) { %>
    <div class="search-results">
        <h3> City: <%= city %> </h3>
        <p> Temperature: <%= apiData.temp %> C</p>
        <p> Feels Like: <%= apiData.feels_like %> C</p>
        <p> Humidity: <%= apiData.humidity %> </p>
        <p> Minimum Temperature: <%= apiData.min_temp %> C</p>
        <p> Maximum Temperature: <%= apiData.max_temp %> C</p>
        <p> Wind Speed: <%= apiData.wind_speed %> Km/Hr</p>
        <p> Wind Degree: <%= apiData.wind_degrees %> </p>
        <p> Cloud Percentage: <%= apiData.cloud_pct %> %</p>
    </div>
    <% } else if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>
    </div>
    <div class="content">
        <h1>WEATHER</h1>
        <p>Find weather of your city.</p>
        <!-- Use a button to pause/play the video with JavaScript -->
        <button id="myBtn" onclick="myFunction()">Pause</button>
      </div>
      <video autoplay muted loop id="myVideo">
        <source src="/videos/weather.mp4" type="video/mp4">
        </video>
      <%- include('./partials/footer') %>