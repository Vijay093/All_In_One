<%- include('./partials/header'); %>

    <div class="content-container">
    <h1><%= pageTitle %></h1>
    <div class="form-container">
    <form action="/jokes" method="post">
        <input type="text" name="limit" placeholder="Type the limit">
        <button type="submit">Get Jokes</button>
    </form>
    </div>
    <% if (apiData) { %>
    <% for (let data of apiData) { %>
    <div class="search-results">
        <p> <%= data.joke %> </p>
    </div>
    <% } %>
    <% } else if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>
    </div>
    <div class="content">
        <h1>JOKES</h1>
        <p>Get Random Jokes.</p>
        <!-- Use a button to pause/play the video with JavaScript -->
        <button id="myBtn" onclick="myFunction()">Pause</button>
      </div>
      <video autoplay muted loop id="myVideo">
        <source src="/videos/joke.mp4" type="video/mp4">
        </video>
      <%- include('./partials/footer') %>